name: C/C++ CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: configure
      run: cmake template -DCMAKE_MODULE_PATH=cmake
    - name: make
      run: make
